# vault-graph requirements

- **Node 22+** (built-in `node:sqlite`).
- **personal-ai** module installed (it reads that module's `memory.db`).
- Obsidian (optional, to browse the vault): `brew install --cask obsidian`.

No third-party accounts or network egress. Cost: free (local).
