# deck-builder requirements

- **Python 3** (system python3 is fine).
- **python-pptx** (`pip3 install python-pptx`) — the installer does this.

No third-party accounts, API keys, or network access at render time. Cost: free.
