# data-import requirements

- **personal-ai** module installed (this writes into its `memory.db`).
- **Node 22+** (uses the built-in `node:sqlite`).
- A local export file from your other assistant (JSONL or Markdown for v1).

No third-party accounts, API keys, or network access are required. Cost: free.
